test = {
  'name': 'Question',
  'points': 1,
  'suites': [
    {
      'cases': [
        {
          'code': r"""
          >>> # It looks like you didn't load an image.  Hint:
          >>> # your code should start like this:
          >>> #   art = IPython.display.Image(...
          >>> import IPython.display
          >>> type(graph) == IPython.core.display.Image
          True
          """,
          'hidden': False,
          'locked': False
        },
      ],
      'scored': True,
      'setup': '',
      'teardown': '',
      'type': 'doctest'
    }
  ]
}
