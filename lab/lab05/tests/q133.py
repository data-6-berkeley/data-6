test = {
  'name': 'Question',
  'points': 1,
  'suites': [
    {
      'cases': [
        {
          'code': r"""
          >>> # Your result isn't the right number.  Population grows by
          >>> # several million per year, so one thing you should check is
          >>> # whether your answer is in the right ballpark.
          >>> import numpy as np
          >>> round(average_population_change) == 72290175.0
          True
          """,
          'hidden': False,
          'locked': False
        }
      ],
      'scored': True,
      'setup': '',
      'teardown': '',
      'type': 'doctest'
    }
  ]
}
